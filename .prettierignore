# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Vendored code — transferred 1:1 from other packages; keep byte-for-byte so it
# can be extracted later without a reformat diff (see src/lib/vendor/*/README.md).
/src/lib/vendor/
